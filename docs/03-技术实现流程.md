# AI 小说创作系统 - 技术实现流程（Next.js 全栈）

## 1. 技术架构

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Next.js)                      │
├─────────────────────────────────────────────────────────────┤
│  UI 组件 (shadcn/ui)  │  状态管理 (Zustand)  │  路由 (App Router) │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     API 层 (Next.js API Routes)              │
├─────────────────────────────────────────────────────────────┤
│  认证中间件  │  业务逻辑  │  数据验证  │  错误处理           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────┬──────────────────────┬───────────────┐
│   数据库层 (Prisma)   │   AI 服务层          │   文件存储     │
├──────────────────────┼──────────────────────┼───────────────┤
│   PostgreSQL         │   OpenAI API         │   Vercel Blob │
│                      │   Anthropic API      │               │
└──────────────────────┴──────────────────────┴───────────────┘
```

### 1.2 技术栈选型

#### 前端技术栈
- **框架**: Next.js 16+ (App Router)
- **语言**: TypeScript 5+
- **样式**: Tailwind CSS 3+
- **UI 组件**: shadcn/ui
- **状态管理**: Zustand
- **表单处理**: React Hook Form + Zod
- **富文本编辑器**: Tiptap
- **图表可视化**: Recharts / React Flow
- **HTTP 客户端**: fetch API / axios

#### 后端技术栈
- **框架**: Next.js API Routes / Server Actions
- **ORM**: Prisma
- **数据库**: PostgreSQL 15+
- **认证**: NextAuth.js v5
- **验证**: Zod
- **AI SDK**: Vercel AI SDK / OpenAI SDK

#### 开发工具
- **包管理**: pnpm
- **代码规范**: ESLint + Prettier
- **Git Hooks**: Husky + lint-staged
- **测试**: Vitest + Testing Library
- **部署**: Vercel

---

## 2. 项目结构

采用 Monorepo 架构，所有应用统一放在 `apps` 目录中，便于未来扩展多个应用。

```
book/
├── apps/                        # 应用目录（Monorepo）
│   └── web/                     # 主 Web 应用
│       ├── src/
│       │   ├── app/             # Next.js App Router
│       │   │   ├── (auth)/      # 认证相关页面（路由组）
│       │   │   │   ├── login/
│       │   │   │   └── register/
│       │   │   ├── (dashboard)/ # 主应用页面（路由组）
│       │   │   │   ├── projects/        # 项目列表
│       │   │   │   ├── project/[id]/    # 项目详情
│       │   │   │   │   ├── settings/    # 核心设定
│       │   │   │   │   ├── characters/  # 角色管理
│       │   │   │   │   ├── outline/     # 大纲管理
│       │   │   │   │   └── chapters/    # 章节创作
│       │   │   │   └── layout.tsx
│       │   │   ├── api/         # API 路由
│       │   │   │   ├── auth/
│       │   │   │   ├── projects/
│       │   │   │   ├── characters/
│       │   │   │   ├── chapters/
│       │   │   │   └── ai/      # AI 生成接口
│       │   │   ├── layout.tsx
│       │   │   └── page.tsx
│       │   ├── components/      # React 组件
│       │   │   ├── ui/          # shadcn/ui 组件
│       │   │   ├── features/    # 功能组件
│       │   │   │   ├── project/
│       │   │   │   ├── character/
│       │   │   │   ├── chapter/
│       │   │   │   └── editor/
│       │   │   └── layout/      # 布局组件
│       │   ├── lib/             # 工具库
│       │   │   ├── prisma.ts    # Prisma 客户端
│       │   │   ├── auth.ts      # 认证配置
│       │   │   ├── ai/          # AI 服务封装
│       │   │   │   ├── client.ts
│       │   │   │   └── prompts/ # Prompt 模板
│       │   │   │       ├── settings.ts
│       │   │   │       ├── character.ts
│       │   │   │       ├── chapter.ts
│       │   │   │       └── outline.ts
│       │   │   ├── utils.ts     # 工具函数
│       │   │   └── validations.ts # 数据验证
│       │   ├── hooks/           # 自定义 Hooks
│       │   ├── stores/          # Zustand 状态管理
│       │   ├── types/           # TypeScript 类型定义
│       │   └── styles/          # 全局样式
│       ├── prisma/
│       │   ├── schema.prisma    # 数据库模型
│       │   └── migrations/      # 数据库迁移
│       ├── public/              # 静态资源
│       ├── tests/               # 测试文件
│       ├── .env.example         # 环境变量示例
│       ├── next.config.js       # Next.js 配置
│       ├── tailwind.config.ts   # Tailwind 配置
│       ├── tsconfig.json        # TypeScript 配置
│       └── package.json         # 应用依赖
├── docs/                        # 项目文档
│   ├── 01-需求规划.md
│   ├── 02-迭代计划.md
│   └── 03-技术实现流程.md
├── package.json                 # 根目录 package.json（workspace 配置）
├── pnpm-workspace.yaml          # pnpm workspace 配置
└── .gitignore
```

### 2.1 Monorepo 配置

在根目录创建 `pnpm-workspace.yaml`：

```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

根目录 `package.json`：

```json
{
  "name": "ai-novel-creator-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "db:generate": "pnpm --filter web db:generate",
    "db:push": "pnpm --filter web db:push",
    "db:migrate": "pnpm --filter web db:migrate",
    "db:studio": "pnpm --filter web db:studio"
  }
}
```

---

## 3. 数据库设计

### 3.1 Prisma Schema

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// 用户表
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  password      String
  avatar        String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  projects      Project[]
  sessions      Session[]
}

// 会话表
model Session {
  id           String   @id @default(cuid())
  userId       String
  token        String   @unique
  expiresAt    DateTime
  createdAt    DateTime @default(now())

  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// 项目表
model Project {
  id           String    @id @default(cuid())
  title        String
  genre        String    // 小说类型
  description  String?
  coverImage   String?
  status       String    @default("draft") // draft, writing, completed
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  userId       String

  user         User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  settings     ProjectSettings?
  characters   Character[]
  outline      Outline?
  chapters     Chapter[]
}

// 项目设定表
model ProjectSettings {
  id           String   @id @default(cuid())
  projectId    String   @unique
  worldView    String   @db.Text // 世界观
  coreConflict String   @db.Text // 核心冲突
  powerSystem  String?  @db.Text // 力量体系
  factions     Json?    // 势力组织
  specialRules Json?    // 特殊规则
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  project      Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
}

// 角色表
model Character {
  id           String   @id @default(cuid())
  projectId    String
  name         String
  role         String   // protagonist, supporting, antagonist
  age          Int?
  gender       String?
  appearance   String?  @db.Text
  personality  String[] // 性格特点
  background   String   @db.Text // 不超过150字
  motivation   String?  @db.Text
  strengths    String[]
  weaknesses   String[]
  relationships Json?   // 人物关系
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  project      Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
}

// 大纲表
model Outline {
  id           String   @id @default(cuid())
  projectId    String   @unique
  structure    Json     // 三幕式结构
  plotPoints   Json     // 情节点
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  project      Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  items        OutlineItem[]
}

// 大纲条目表
model OutlineItem {
  id           String   @id @default(cuid())
  outlineId    String
  chapterNumber Int
  title        String
  summary      String   @db.Text
  keyEvents    String[]
  characters   String[] // 角色ID列表
  order        Int
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  outline      Outline  @relation(fields: [outlineId], references: [id], onDelete: Cascade)
}

// 章节表
model Chapter {
  id           String   @id @default(cuid())
  projectId    String
  chapterNumber Int
  title        String
  content      String   @db.Text
  wordCount    Int      @default(0)
  status       String   @default("draft") // draft, completed
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  project      Project  @relation(fields: [projectId], references: [id], onDelete: Cascade)
  versions     ChapterVersion[]
}

// 章节版本表
model ChapterVersion {
  id           String   @id @default(cuid())
  chapterId    String
  content      String   @db.Text
  wordCount    Int
  note         String?
  createdAt    DateTime @default(now())

  chapter      Chapter  @relation(fields: [chapterId], references: [id], onDelete: Cascade)
}
```

---

## 4. 核心功能实现

### 4.1 AI 服务封装

```typescript
// src/lib/ai/client.ts

import OpenAI from 'openai';
import Anthropic from '@anthropic-ai/sdk';

export class AIService {
  private openai: OpenAI;
  private anthropic: Anthropic;

  constructor() {
    this.openai = new OpenAI({
      apiKey: process.env.OPENAI_API_KEY,
    });

    this.anthropic = new Anthropic({
      apiKey: process.env.ANTHROPIC_API_KEY,
    });
  }

  async generateWithOpenAI(prompt: string, options?: {
    model?: string;
    temperature?: number;
    maxTokens?: number;
  }) {
    const response = await this.openai.chat.completions.create({
      model: options?.model || 'gpt-4-turbo-preview',
      messages: [{ role: 'user', content: prompt }],
      temperature: options?.temperature || 0.7,
      max_tokens: options?.maxTokens || 2000,
    });

    return response.choices[0].message.content;
  }

  async generateWithClaude(prompt: string, options?: {
    model?: string;
    temperature?: number;
    maxTokens?: number;
  }) {
    const response = await this.anthropic.messages.create({
      model: options?.model || 'claude-3-opus-20240229',
      max_tokens: options?.maxTokens || 2000,
      temperature: options?.temperature || 0.7,
      messages: [{ role: 'user', content: prompt }],
    });

    return response.content[0].text;
  }

  // 流式生成
  async *streamGenerate(prompt: string, provider: 'openai' | 'claude' = 'openai') {
    if (provider === 'openai') {
      const stream = await this.openai.chat.completions.create({
        model: 'gpt-4-turbo-preview',
        messages: [{ role: 'user', content: prompt }],
        stream: true,
      });

      for await (const chunk of stream) {
        const content = chunk.choices[0]?.delta?.content;
        if (content) yield content;
      }
    } else {
      const stream = await this.anthropic.messages.stream({
        model: 'claude-3-opus-20240229',
        max_tokens: 2000,
        messages: [{ role: 'user', content: prompt }],
      });

      for await (const chunk of stream) {
        if (chunk.type === 'content_block_delta' && chunk.delta.type === 'text_delta') {
          yield chunk.delta.text;
        }
      }
    }
  }
}

export const aiService = new AIService();
```

### 4.2 Prompt 模板管理

```typescript
// src/lib/ai/prompts/settings.ts

export const generateWorldViewPrompt = (params: {
  genre: string;
  keywords: string[];
  style: string;
}) => {
  return `你是一位资深小说策划师。请根据以下信息生成小说的核心世界观设定：

类型：${params.genre}
关键词：${params.keywords.join('、')}
风格：${params.style}

请生成以下内容（总字数300-500字）：
1. 世界观背景（时代、地点、社会结构）
2. 核心冲突（主要矛盾）
3. 特殊设定（修炼体系/科技体系/魔法规则等）
4. 主要势力或组织

要求：
- 设定要有创新性和吸引力
- 逻辑自洽，避免矛盾
- 为后续情节发展留有空间
- 语言简洁生动

请直接输出设定内容，不要额外的解释。`;
};
```

```typescript
// src/lib/ai/prompts/chapter.ts

export const generateChapterPrompt = (params: {
  projectSettings: string;
  characters: Array<{ name: string; background: string }>;
  outlineSummary: string;
  previousChapter?: string;
  chapterOutline: string;
  wordCount: number;
  perspective: 'first' | 'third';
}) => {
  return `你是一位专业小说作家。请根据以下信息创作小说章节：

【世界观设定】
${params.projectSettings}

【主要角色】
${params.characters.map(c => `${c.name}：${c.background}`).join('\n')}

【故事大纲】
${params.outlineSummary}

${params.previousChapter ? `【上一章节内容摘要】\n${params.previousChapter}\n` : ''}

【本章大纲】
${params.chapterOutline}

创作要求：
1. 字数：约${params.wordCount}字
2. 视角：${params.perspective === 'first' ? '第一人称' : '第三人称'}
3. 情节要跌宕起伏，有冲突和转折
4. 人物对话要符合角色性格
5. 场景描写要生动具体
6. 保持与前文的连贯性
7. 语言流畅，富有感染力
8. 章节结尾要有悬念或钩子

请直接输出章节内容，不要包含章节标题和额外说明。`;
};
```

### 4.3 API 路由实现

```typescript
// src/app/api/ai/generate-settings/route.ts

import { NextRequest, NextResponse } from 'next/server';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import { aiService } from '@/lib/ai/client';
import { generateWorldViewPrompt } from '@/lib/ai/prompts/settings';
import { z } from 'zod';

const requestSchema = z.object({
  genre: z.string(),
  keywords: z.array(z.string()),
  style: z.string(),
});

export async function POST(req: NextRequest) {
  try {
    // 认证检查
    const session = await getServerSession(authOptions);
    if (!session) {
      return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
    }

    // 参数验证
    const body = await req.json();
    const params = requestSchema.parse(body);

    // 生成 Prompt
    const prompt = generateWorldViewPrompt(params);

    // 调用 AI 生成
    const content = await aiService.generateWithOpenAI(prompt, {
      temperature: 0.8,
      maxTokens: 1500,
    });

    return NextResponse.json({ content });
  } catch (error) {
    console.error('Generate settings error:', error);

    if (error instanceof z.ZodError) {
      return NextResponse.json(
        { error: 'Invalid parameters', details: error.errors },
        { status: 400 }
      );
    }

    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}
```

```typescript
// src/app/api/ai/generate-chapter/route.ts

import { NextRequest } from 'next/server';
import { getServerSession } from 'next-auth';
import { authOptions } from '@/lib/auth';
import { aiService } from '@/lib/ai/client';
import { generateChapterPrompt } from '@/lib/ai/prompts/chapter';
import { prisma } from '@/lib/prisma';

export async function POST(req: NextRequest) {
  const session = await getServerSession(authOptions);
  if (!session) {
    return new Response('Unauthorized', { status: 401 });
  }

  const { projectId, chapterOutline, wordCount, perspective } = await req.json();

  // 获取项目相关数据
  const project = await prisma.project.findUnique({
    where: { id: projectId },
    include: {
      settings: true,
      characters: true,
    },
  });

  if (!project || project.userId !== session.user.id) {
    return new Response('Forbidden', { status: 403 });
  }

  // 构建 Prompt
  const prompt = generateChapterPrompt({
    projectSettings: project.settings?.worldView || '',
    characters: project.characters.map(c => ({
      name: c.name,
      background: c.background,
    })),
    outlineSummary: '', // 从大纲获取
    chapterOutline,
    wordCount,
    perspective,
  });

  // 流式返回
  const encoder = new TextEncoder();
  const stream = new ReadableStream({
    async start(controller) {
      try {
        for await (const chunk of aiService.streamGenerate(prompt)) {
          controller.enqueue(encoder.encode(chunk));
        }
        controller.close();
      } catch (error) {
        controller.error(error);
      }
    },
  });

  return new Response(stream, {
    headers: {
      'Content-Type': 'text/plain; charset=utf-8',
      'Transfer-Encoding': 'chunked',
    },
  });
}
```

---

## 5. 前端实现

### 5.1 状态管理（Zustand）

```typescript
// src/stores/projectStore.ts

import { create } from 'zustand';
import { Project, Character, Chapter } from '@prisma/client';

interface ProjectStore {
  currentProject: Project | null;
  characters: Character[];
  chapters: Chapter[];

  setCurrentProject: (project: Project) => void;
  setCharacters: (characters: Character[]) => void;
  setChapters: (chapters: Chapter[]) => void;

  addCharacter: (character: Character) => void;
  updateCharacter: (id: string, data: Partial<Character>) => void;
  deleteCharacter: (id: string) => void;

  addChapter: (chapter: Chapter) => void;
  updateChapter: (id: string, data: Partial<Chapter>) => void;
  deleteChapter: (id: string) => void;
}

export const useProjectStore = create<ProjectStore>((set) => ({
  currentProject: null,
  characters: [],
  chapters: [],

  setCurrentProject: (project) => set({ currentProject: project }),
  setCharacters: (characters) => set({ characters }),
  setChapters: (chapters) => set({ chapters }),

  addCharacter: (character) =>
    set((state) => ({ characters: [...state.characters, character] })),

  updateCharacter: (id, data) =>
    set((state) => ({
      characters: state.characters.map((c) =>
        c.id === id ? { ...c, ...data } : c
      ),
    })),

  deleteCharacter: (id) =>
    set((state) => ({
      characters: state.characters.filter((c) => c.id !== id),
    })),

  addChapter: (chapter) =>
    set((state) => ({ chapters: [...state.chapters, chapter] })),

  updateChapter: (id, data) =>
    set((state) => ({
      chapters: state.chapters.map((c) =>
        c.id === id ? { ...c, ...data } : c
      ),
    })),

  deleteChapter: (id) =>
    set((state) => ({
      chapters: state.chapters.filter((c) => c.id !== id),
    })),
}));
```

### 5.2 核心组件示例

```typescript
// src/components/features/chapter/ChapterGenerator.tsx

'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Textarea } from '@/components/ui/textarea';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { useProjectStore } from '@/stores/projectStore';

export function ChapterGenerator() {
  const { currentProject } = useProjectStore();
  const [outline, setOutline] = useState('');
  const [wordCount, setWordCount] = useState(2300);
  const [content, setContent] = useState('');
  const [isGenerating, setIsGenerating] = useState(false);

  const handleGenerate = async () => {
    if (!currentProject) return;

    setIsGenerating(true);
    setContent('');

    try {
      const response = await fetch('/api/ai/generate-chapter', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          projectId: currentProject.id,
          chapterOutline: outline,
          wordCount,
          perspective: 'third',
        }),
      });

      if (!response.ok) throw new Error('Generation failed');

      const reader = response.body?.getReader();
      const decoder = new TextDecoder();

      while (true) {
        const { done, value } = await reader!.read();
        if (done) break;

        const chunk = decoder.decode(value);
        setContent((prev) => prev + chunk);
      }
    } catch (error) {
      console.error('Generation error:', error);
    } finally {
      setIsGenerating(false);
    }
  };

  return (
    <div className="space-y-4">
      <div>
        <Label>章节大纲</Label>
        <Textarea
          value={outline}
          onChange={(e) => setOutline(e.target.value)}
          placeholder="描述本章的主要情节..."
          rows={4}
        />
      </div>

      <div>
        <Label>目标字数</Label>
        <Input
          type="number"
          value={wordCount}
          onChange={(e) => setWordCount(Number(e.target.value))}
          min={1500}
          max={5000}
        />
      </div>

      <Button
        onClick={handleGenerate}
        disabled={!outline || isGenerating}
      >
        {isGenerating ? '生成中...' : '生成章节'}
      </Button>

      {content && (
        <div className="mt-4 p-4 border rounded-lg">
          <div className="whitespace-pre-wrap">{content}</div>
          <div className="mt-2 text-sm text-gray-500">
            当前字数：{content.length}
          </div>
        </div>
      )}
    </div>
  );
}
```

---

## 6. 部署流程

### 6.1 环境变量配置

```bash
# .env.example

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/novel_ai"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key"

# AI APIs
OPENAI_API_KEY="sk-..."
ANTHROPIC_API_KEY="sk-ant-..."

# File Storage (Vercel Blob)
BLOB_READ_WRITE_TOKEN="vercel_blob_..."
```

### 6.2 Vercel 部署步骤

1. **连接 GitHub 仓库**
   - 在 Vercel 控制台导入项目
   - 选择 GitHub 仓库

2. **配置环境变量**
   - 在 Vercel 项目设置中添加所有环境变量
   - 确保生产环境和预览环境都配置正确

3. **配置数据库**
   - 使用 Vercel Postgres 或外部 PostgreSQL
   - 运行数据库迁移：`npx prisma migrate deploy`

4. **构建设置**
   - Build Command: `pnpm build`
   - Output Directory: `.next`
   - Install Command: `pnpm install`

5. **部署**
   - 推送代码到 main 分支自动部署
   - 或手动触发部署

### 6.3 数据库迁移

```bash
# 开发环境
npx prisma migrate dev --name init

# 生产环境
npx prisma migrate deploy

# 生成 Prisma Client
npx prisma generate
```

---

## 7. 性能优化

### 7.1 前端优化
- 使用 Next.js Image 组件优化图片
- 代码分割和懒加载
- 使用 React.memo 避免不必要的重渲染
- 实现虚拟滚动（长列表）
- 使用 SWR 或 React Query 缓存数据

### 7.2 后端优化
- 数据库查询优化（索引、N+1 问题）
- API 响应缓存（Redis）
- AI 请求防抖和节流
- 使用 Edge Functions 降低延迟

### 7.3 AI 成本优化
- 缓存常见生成结果
- 优化 Prompt 减少 Token 消耗
- 使用更便宜的模型处理简单任务
- 实现用户配额限制

---

## 8. 安全措施

- 使用 NextAuth.js 实现安全认证
- API 路由添加认证中间件
- 使用 Zod 验证所有输入
- 防止 SQL 注入（Prisma 自动处理）
- 实现 CSRF 保护
- 敏感数据加密存储
- 限制 API 调用频率（Rate Limiting）

---

## 9. 监控与日志

- 使用 Vercel Analytics 监控性能
- 集成 Sentry 错误追踪
- 记录 AI API 调用日志
- 监控数据库性能
- 设置告警机制

---

## 10. 开发规范

### 10.1 代码规范
- 使用 TypeScript 严格模式
- 遵循 ESLint 和 Prettier 规则
- 组件使用函数式写法
- 使用有意义的变量和函数名
- 添加必要的注释

### 10.2 Git 工作流
- main: 生产环境
- develop: 开发环境
- feature/*: 功能分支
- 使用 Conventional Commits 规范提交信息

### 10.3 测试策略
- 单元测试：核心业务逻辑
- 集成测试：API 路由
- E2E 测试：关键用户流程
- 目标覆盖率：≥ 70%
